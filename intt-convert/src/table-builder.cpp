#include "table-builder.h"

namespace intt { namespace impl {

#if 0
	Table::Table(size_t cols, size_t rows)
	{
		columns.resize(cols);
		this->rows.resize(rows);
	}

	void Table::addCell(const TabCell& cell)
	{
		
	}
#endif

}}
